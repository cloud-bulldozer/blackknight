---
  - name: Setup host for minikube
    hosts: kube
    remote_user: root
    roles:
      - role: repos
      - role: hypervisor
      - role: go
      - role: dep
      - role: kubectl
      - role: minikube

  # WIP:
  # - name: Deploy k8s
  #   hosts: kube
  #   roles:
  #     - role: minikube_start
  #     - role: minikube_prepare
